// creating object
const Calculator = {
  displayValue: "0",
  firstOperand: null,
  waitSecondOperand: false,
  operator: null,
};

function inputDigit(digit) {
  const { displayValue, waitSecondOperand } = Calculator;
}
